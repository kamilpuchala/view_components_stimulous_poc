<% unless current_user?(user) %>
  <div id="follow_form">
  <% if current_user.following?(user) %>
    <%= render User::Following::Unfollow::Component.new(user: @user) %>
  <% else %>
    <%= render User::Following::Follow::Component.new(user: @user) %>
  <% end %>
  </div>
<% end %>
