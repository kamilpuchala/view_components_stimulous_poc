<li data-controller="user">
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= button_to "delete", user, method: :delete, remote: true, data: { action: "click->user--component#remove" } %>
  <% end %>
</li>
